#include "Player.h"

Player::Player()
{
    Player::updateDate();
    return;
}

bool Player::updateDate()
{
    /*iHealth = Read<int>(local+hazedumper::netvars::m_iHealth);
    iArmor = Read<int>(local+hazedumper::netvars::m_ArmorValue);
    fFlags = Read<int>(local+hazedumper::netvars::m_fFlags);
    flFlashed = Read<bool>(local+hazedumper::netvars::m_flFlashDuration);
*/
    return true;
}

